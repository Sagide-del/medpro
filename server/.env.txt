# ---- Server ----
PORT=5000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# ---- Database ----
# NOTE: config/database.js reads DATABASE_URL (not DB_HOST/DB_PORT/etc).
# Adjust user/password/db name to match your local Postgres setup.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/medpro

# ---- Auth ----
JWT_SECRET=change-me-to-a-long-random-string-before-any-shared-deploy
JWT_EXPIRES_IN=12h

# ---- M-Pesa Daraja (Safaricom) ----
# Left as placeholders: purchases auto-complete in local dev without a real
# Daraja account. Fill in real sandbox/production values to go live.
MPESA_ENV=sandbox
MPESA_CONSUMER_KEY=your_consumer_key
MPESA_CONSUMER_SECRET=your_consumer_secret
MPESA_SHORTCODE=174379
MPESA_PASSKEY=your_passkey
MPESA_CALLBACK_URL=https://yourdomain.co.ke/api/payments/mpesa/callback

# ---- SMS (Africa's Talking) / WhatsApp (Meta Cloud API) ----
# Left as placeholders: group alerts simulate SMS/WhatsApp sends locally.
AT_API_KEY=your_africastalking_api_key
AT_USERNAME=your_africastalking_username
WHATSAPP_TOKEN=your_whatsapp_cloud_api_token
WHATSAPP_PHONE_ID=your_whatsapp_phone_number_id

# ---- Logging ----
LOG_LEVEL=info
